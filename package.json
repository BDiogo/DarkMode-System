{
  "name": "darkmodesystem",
  "description": "",
  "version": "1.0.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "DarkModeSystem",
    "manifest": "src/manifest.json",
    "main": "darkmodesystem.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BDiogo/DarkMode-System.git"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.7.0"
  },
  "dependencies": {
    "@skpm/dialog": "^0.3.0"
  }
}
